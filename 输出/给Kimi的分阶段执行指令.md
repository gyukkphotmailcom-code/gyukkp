# 给 Kimi 的分阶段执行指令

## 总控指令（第一次先完整发送）

你是本项目的代码执行者，不是产品策划。不要重新设计玩法，也不要凭印象“优化”原版。

开始前必须完整阅读：

1. `/Users/xingbili/Downloads/kimi/阳阳游泳游戏/开发规格书.md`
2. `/Users/xingbili/Downloads/kimi/阳阳游泳游戏/输出/原版画面参考与测量.md`
3. `/Users/xingbili/Downloads/kimi/阳阳游泳游戏/过程文件/原版参考/官方截图/`
4. `/Users/xingbili/Downloads/kimi/阳阳游泳游戏/过程文件/原版参考/视频帧/usa-swim-frames/`

保留旧文件 `阳阳游泳大赛.html`，不得覆盖。新文件只能写到：

`/Users/xingbili/Downloads/kimi/阳阳游泳游戏/阳阳水泳大乱斗-原版复刻.html`

技术要求：最终运行物是单个HTML、零运行时外部库、256×224逻辑Canvas、固定60Hz逻辑更新、整数像素、关闭图像平滑。Python、Pillow等只可作为构建期工具；构建脚本、输入清单、输入哈希和最终生成物必须能从干净Git基线复现。若原始照片或录像因隐私/版权被 `.gitignore` 排除，必须提交合法的派生像素基准或让核心构建在缺少本地参考时仍可运行，不能让拟提交脚本暗中依赖未跟踪文件。照片只允许在本地预览页中使用同目录相对引用。开发期间可以生成辅助截图或测试页，但全部放入 `过程文件/Kimi开发/`。

当前批准基线只到 `c99e84e stage1-approved-r3` 和 `1a01918 stage2-approved-r3`。阶段1场景/HUD与阶段2身份头像不得重画。当前工作区里的阶段3/R6只能作为逻辑候选，阶段3A旧稿尚未通过；不得将其命名为 `approved`，也不得用 `git add .` 混合提交。

必须分阶段执行。每完成一个阶段：

- 停止继续开发；
- 报告改动内容和文件位置；
- 列出本阶段验收结果；
- 明确仍未实现的内容；
- 等待“进入下一阶段”指令。

禁止一次性写完整游戏，禁止把“能运行”当作“已经复刻”。

---

## 阶段1：只复刻静态比赛画面

### 给 Kimi 的指令

现在只执行阶段1，不写移动、攻击、AI、粒子、声音和触屏。

目标：双击新HTML后，直接显示一张与日版原作构图一致的静态比赛画面。

必须完成：

- Canvas逻辑尺寸256×224，CSS默认整数放大3倍。
- 比赛区 `y=0..167`，HUD `y=168..223`。
- 日版建筑和富士山远景。
- `y≈72..101` 的透视水面。
- `y≈102..165` 的透视水下池体。
- 中央黑白浮球泳道绳及水下延伸线。
- 底部黑底白框HUD。
- HUD显示 `1P 阳阳 / TIME / CPU 爸爸`、两组HP/Oxy、初始 `3:00`。
- 两个临时原版比例像素小人，先不用照片脸。

禁止：渐变、圆角、阴影、半透明水罩、系统字体、顶部HUD、红色血条、自由矩形水箱。

验收时必须把浏览器截图与 `官方截图/jp-action-e.gif` 以及实机帧 `06m45s.png` 并排检查，并报告每个区域的坐标。

### 阶段1通过条件

- 静态画面构图没有明显偏差。
- HUD位置、大小和顺序正确。
- 画面全文搜索没有 `createLinearGradient`、`roundRect`、`globalAlpha`、`ellipse`。
- 浏览器缩放时仍保持像素清晰。

---

## 阶段2：制作并确认阳阳/爸爸像素头像

### 给 Kimi 的指令

现在只执行阶段2，不开发战斗。

依据：

- 阳阳：`17296.jpeg`，参考裁剪 `{sx:530, sy:480, sw:340, sh:340}`。
- 爸爸：`IMG_0387.jpeg`，参考裁剪 `{sx:1214, sy:880, sw:1100, sh:1100}`。

制作 `过程文件/Kimi开发/头像预览.html`，同时展示原照片裁剪、24×24头像原尺寸和8倍放大预览。

头像不是圆形照片缩图。应先将照片降采样和有限色量化，再人工用像素块明确重绘识别特征：

- 阳阳：儿童圆脸、泳帽、泳镜/镜带、短下巴。
- 爸爸：成年脸型、眼镜框/镜腿、发型或发际线、微笑。

每人最多使用透明色、轮廓色、3档肤色、2档头发/泳帽色、1个强调色。输出正常、挨打、气绝、败北4种表情。头像像素数组是后续动作生成器使用的唯一身份母版，不代表运行时要把同一24×24整头机械贴到每种身体上；运行时不能重新做平滑照片裁切。

### 阶段2通过条件

- 隐藏姓名与队色，仍能分辨阳阳和爸爸。
- 放大后没有摄影噪点、柔边和半透明像素。
- 头像与原版身体的像素颗粒和描边粗细一致。
- 用户确认头像后才能进入阶段3。

---

## 阶段3：原版移动、跳跃和换气

### 给 Kimi 的指令

实现固定60Hz状态机，但暂不实现攻击与AI。玩家和爸爸都先使用测试输入或静止脚本。

必须完成：

- 水面待机和换气。
- 水面A跳跃，起跳前左右决定方向。
- 按住↓下潜。
- 水下八向移动。
- 水下移动配合A连续操作提高速度；不做技能式冲刺和冷却。
- 浮出水面、自然恢复Oxy、B超级换气。
- 水下人物切换青蓝专用色版。
- 气泡与入水水花只使用像素精灵。

从参考录像为每个待标定动作寻找至少3个干净实例，记录开始帧、结束帧、位移、循环帧数、版本和源帧率。只有两段时只够探索，不能宣称完成原版标定。30fps证据按约2个60Hz tick的粒度记录；无法确认的参数保留在 `ORIGINAL_TUNING`，不得散落硬编码。

### 阶段3通过条件

- 所有坐标为整数。
- 60Hz固定步进，不使用可变dt直接推进物理。
- 无输入时不会自动向终点前进。
- 没有镜头横向滚动、距离和终点。

---

## 阶段3A-0：完整人物精灵工艺验证（只做3个原型，不改主HTML）

### 强制拆门：先像人，再像阳阳/爸爸

本阶段必须严格分成两个小门，不能在同一轮里混做：

1. **R3A 原版人体母版门**：只输出原版人物母版，不读取/粘贴阶段2 `FACES`，不出现
   阳阳、爸爸、队色或身份像素。母版必须从锁定的原版裁剪和逐像素 clean subject mask
   生成；禁止手写大色块身体、全局 `fill_holes`、矩形补肢体或重复铺头像行。
2. **R3B 身份层门**：只有用户/Codex明确确认 R3A 的3个姿态在原生1×下都像完整的人，
   才能为每个动作单独加入阳阳的泳帽/泳镜和爸爸的发型/眼镜。身份修改必须限制在另行
   审批的唯一头部内部 mask；该 mask 外相对R3A母版逐像素差异必须为0。

R3A/R3B均不改主HTML。R3B未通过前禁止进入3A-1。

### 给 Kimi 的指令

先停止扩展旧 `build_sprites_3a.py` 的18张稿件。本小阶段只重做以下3个代表姿态：`SURF0`、`JUMP_AIR`、`UW_NORMAL`。三者分别验证水面、空中展开和横向水下姿态；不得改主HTML、状态机、物理、Oxy或输入。

硬性要求：

- 验收顺序是“完整人形/原版人体结构”优先于“像阳阳/爸爸”。若人体母版不通过，
  即使身份辨识很强也必须返工，不得用头像特征掩盖身体错误。
- 24×24阶段2头像只作为阳阳/爸爸的身份特征母版，不得整块当作所有动作的固定头部，也不得再拼接16×16小身体。
- 每个姿态从原版原生256×224参考中先描完整人物轮廓，再在动作头部内部替换泳帽/泳镜、发型/眼镜和五官；颈肩、头部朝向、躯干和四肢必须保持原版动作结构。
- 阳阳和爸爸分别输出完整人物精灵；除身份区和辅助队色外，两人的身体轮廓来自同一原版角色模板。
- 每帧声明可变 `w/h`、世界/root锚点、脚底或身体锚点、身份替换区、可见外接框及水面遮挡层。
- 水面调色和遮挡按透视水面 `y≈72..101` 与水下起点 `y≈102` 的真实层级处理，禁止使用固定 `y=96` 把人物水平切成两种颜色。
- 左右方向都要出图；不能只用运行时镜像。泳镜、眼镜、高光和肢体前后关系需要人工修正。
- 核心生成物必须在缺少被忽略的本地参考图时仍可从干净Git基线确定性重建；参考图只影响审核板生成时，脚本应明确跳过并报告，不能先崩溃。

R3A只交付：独立人体母版生成器、3姿态 clean subject/manifest/母版像素数据、原版与
母版同尺度1×对照、8×最近邻图、**包含内部调色类别的全像素差异图**、隐藏未来身份区后的
人体图、左右方向候选表和构建复现记录。不得交付阳阳/爸爸稿冒充R3A。

R3B只交付：每姿态单独的身份允许mask、阳阳/爸爸×左右稿、相对R3A母版的changed-pixel
差异图，以及自动断言 `changedPixels ⊆ approvedIdentityMask`、`outsideDiff == 0`。

### 阶段3A-0通过条件

- R3A 的 alpha 必须与逐像素确认的 clean subject 完全相等；不能再用10%/15%的宽松
  轮廓XOR替代主体确认。清晰参考下，母版每个内部像素必须等于“锁定裁剪+subject+固定
  语义分类器”的结果；若只能取得压缩视频，必须明确标 `provisional`，不能称ROM逐色真值。
- R3A 自动测试之外，用户/Codex必须先对不带姓名、队色和身份层的1×盲审图人工确认
  “一眼是一个完整的人及对应动作”；发现双脸、重复头、断颈、胶囊或大头加尾巴即阻断。
- 三个姿态在原生1×下均能读成完整游泳人物，不呈现“大头＋尾巴”“火箭”或细小人偶。
- R3B中所有身份修改都位于逐像素批准的唯一身份mask内；mask外相对R3A母版
  `RGBA/调色类别/alpha` 差异全部为0，且不改变颈肩、手脚、泳衣、root或visibleBBox。
- 跨帧锚点、头身接口和水层遮挡没有跳变、断缝或水平硬切色；阳阳/爸爸在R3B后仍可辨，
  且阶段2头像源文件未被修改。
- 未经用户/Codex明确批准，禁止进入3A-1或接入主HTML。

---

## 阶段3A-1：扩展原版移动动作库（仍不改主HTML）

只有3A-0通过后才执行。使用相同工艺扩展 `SURF/BREATH/JUMP/DIVE/UW/EMERGE` 全套移动动作；加速游泳只有取得原版独立姿态证据时才单列，否则只改变经标定的节奏/位移，不自创BOOST姿势。

每个动作必须提供对应的连续原版帧，不得让 `JUMP0/JUMP1/JUMP2` 共用一张只证明其中一个姿态的参考图。下潜必须覆盖横卧、转身、竖直入水；上浮和水下上下方向必须保证头身朝向一致。完成后停止，等待整套美术门验收。

---

## 阶段3B：只集成已批准的移动精灵

只把阶段3A通过的精灵、manifest、动作锚点和水面遮挡接入主HTML。保留阶段3现有的固定60Hz、定点/整数坐标、输入边沿、状态转移、Oxy和自测骨架，不借机重写玩法。

集成前后使用相同输入回放比较逐tick状态hash；除因新动作锚点明确记录的视觉适配外，位置、状态、氧气和计时结果必须一致。现有 `BODY/LAY/curBody/curHead/drawSwimmers/worldBBox` 属于需替换的旧美术耦合层，不得把硬编码16×16假设带入新资源。

完成后停止；阶段3整体只有在逻辑和新移动精灵同时通过后才能建立 `stage3-approved-*` 基线。

---

## 阶段3C：控制来源解耦与本地双人输入骨架

本阶段在阶段3B移动精灵集成通过后执行，并且必须早于阶段4战斗内核和阶段5爸爸AI。不得在当前
R3B-I人物身份定稿期间提前实施。

只重构输入外围，不修改获批角色动作、物理、Oxy、精灵、root、锚点或状态转移。保留
`tickChar(character, inputSnapshot)` 作为双方共同入口，把当前单一 `Input` 和
`tickChar(dad, IDLE_INPUT)` 改为两份逐 tick 冻结的角色输入快照。控制来源统一支持
`P1 / P2 / CPU / REPLAY`；本阶段只实现 P1、P2 和双角色回放，CPU 先返回空输入，留待阶段5接入。
改造前先检查当前候选代码是否对同一方向在一个tick内重复消费；已知水面 `inp.L` 分支曾出现连续重复，
必须在最近一次阶段3逻辑复审中作为独立缺陷修正并重新建立hash基线，禁止把左右速度不对称固化为兼容行为。

模式固定为：`1P` = 阳阳由玩家控制、爸爸暂用空 CPU 输入；`2P 1V1` = 阳阳和爸爸均由真人控制，
爸爸不运行AI。1P继续兼容方向键 + Z/Space(A) + X(B)；2P模式默认使用阳阳
W/A/S/D + F(A) + G(B)，爸爸方向键 + K(A) + L(B)。键位集中定义在
`CONTROL_BINDINGS`，基于 `KeyboardEvent.code`，每名角色分别计算按住态和 A/B 新按边沿。
HUD右侧在1P显示 `CPU`、在2P显示 `2P`，除该标签外不改HUD坐标和画面。

新增确定性双输入回放测试，覆盖双方同 tick 反向移动、同时跳跃、同时下潜、同时游动+A边沿、
一方换气而另一方移动，以及松键后各自边沿不串号。相同角色、相同输入序列在1P/P2/REPLAY来源下
必须产生相同逐 tick 状态hash。完成后停止，不写攻击、不写AI决策、不做模式菜单美术、不写阶段4B战斗逻辑。

### 阶段3C通过条件

- 两套键盘输入可同时独立操纵两名角色，按住态和边沿不会串到另一人。
- `tickChar`、动作参数和角色状态结构没有按玩家身份复制分支。
- 2P模式下没有任何AI更新；1P的CPU占位只通过统一输入接口返回空输入。
- 固定60Hz、回放和现有阶段3单人测试继续通过；双输入回放可以稳定复现相同hash。
- 完成后等待复审，未通过不得开始阶段4B战斗逻辑。

---

## 阶段4A：战斗证据与成对动作美术门（不写战斗逻辑）

### 给 Kimi 的指令

先为以下动作建立连续证据和动作图集，不改主战斗逻辑：

- 水下踢击与水面/水下受击；
- 肩组双方、肩组出拳双方及肩组挣扎；
- 拉脚者与被拉者；
- 骑乘者、被骑者及按↓下潜脱身；
- Oxy归零气绝、持续掉HP与HP归零败北。

每个单人动作记录 `startup/contact/active/follow-through/recovery`；每个抓取动作制作唯一的成对 `PAIR_CLIP`，逐帧同时记录双方sprite、共同锚点、相对dx/dy、朝向、绘制顺序、hit/hurt/attach框和事件marker。每个需要标定的动作寻找至少3个干净实例；不足时保留 `待标定`。

交付逐动作1×/8×/XOR对照和双人同步GIF/帧条。美术门未通过不得接入主HTML。

---

## 阶段4B：同步战斗内核＋踢击/受击纵切

实现 `旧状态snapshot → 双方intent → 统一解析接触/事件 → 原子commit`，禁止先更新阳阳并直接修改爸爸，再更新爸爸。第一轮只接入水下踢击和受击：

- 明确startup/active/recovery；
- 使用逐帧attack/hurt box，不用透明像素外接框；
- 同一攻击实例对同一目标只命中一次；
- 支持同一tick双方交易命中；
- 伤害、耗氧、击退、硬直、hitstop和无敌帧全部引用 `ORIGINAL_TUNING`，无证据值保持TBD。

完成后停止并提交确定性输入回放、判定框录屏和逐tick事件日志。

---

## 阶段4C：肩组与肩组出拳

使用唯一 `interaction` 和共享 `pairTick/pairAnchor` 驱动双方。进入、换相、出拳命中、中断和释放必须原子发生；双方不能各跑独立动画计时器。拳击事件按 `(interactionId,eventId)` 去重。完成后停止，检查双方互指ID/角色职责、接触点稳定性和所有中断边界。

---

## 阶段4D：拉脚、跳跃骑乘与下潜脱身

实现：

1. 位于水面敌人下方按↑+B进入拉脚；
2. 跳跃角色从上向下扫掠命中目标可骑区域才进入骑乘；
3. 被骑者按住↓按原版过程下潜脱身；
4. 拉脚/骑乘期间的耗氧由成对clip的marker或经标定schedule唯一触发。

不得使用原创百分比挣脱槽，也不得用没有原版证据的自动超时偷偷改变玩法。完成后停止并交付双方同步帧条与接触点误差记录。

---

## 阶段4E：气绝、持续掉HP、败北和中断优先级

Oxy归零后进入气绝，失去正常行动能力并按原版持续掉HP；HP归零进入败北。明确并测试统一优先级（例如KO、气绝、受击、双人交互、普通移动），证据不足的次序标为TBD。任何KO、暂停或重开都不能留下悬挂interaction。

---

## 阶段4F：确定性与原版标定认证

只做回归与标定，不增加新招式：

- 交换阳阳/爸爸内部更新顺序，结果不变；
- 同tick双方命中、每事件只触发一次；
- 1000tick以上固定输入回放的逐tick状态hash完全一致；
- 暂停、重开、边界、负数、穿墙和interaction中断全部通过；
- 将实现画面降采样到证据源帧率，再对动作起止、位移、相位和接触点；30fps证据允许约±2tick，不能冒充60Hz单tick精度。

禁止合并踢击和肩组出拳；禁止加入连打挣脱百分比槽；禁止让缺氧者半速自由战斗。

碰撞框、有效帧、硬直、击退和槽变化全部集中在 `ORIGINAL_TUNING`。开启调试模式时用1px矩形显示判定框，正常模式必须隐藏。还有TBD时可以判“工程骨架通过”，但不得判“ORIGINAL标定通过”。本阶段不做AI、音频或阶段5内容。

### 阶段4整体通过条件

- 每种输入在不同状态下只触发正确动作。
- 肩组、骑乘、拉脚和气绝能正确进入与退出。
- 不会出现双方永久锁死、穿墙、重复伤害或数值变成负数。
- 双人接触点全段稳定，动作轮廓、相位和位移已按可用证据验收。
- 阶段4A至4F必须逐个停下等待复审，禁止一次性全部实现。

---

## 阶段5：完整单场流程、模式选择与爸爸AI

### 给 Kimi 的指令

实现原版式单场流程：

- 标题选择 `1P / 2P 1V1`；1P再选择 `ORIGINAL / KID_ASSIST`，2P不显示AI难度。
- 入场/开场。
- 3:00倒计时。
- HP归零判负。
- 时间到按HP判胜。
- 原版式 `WINNER` 结果演出。
- R重开、Enter暂停。

爸爸AI默认使用 `ORIGINAL` 参数，不加入15帧故意迟钝、20%打空、90帧强制呼吸等旧设计。另设默认关闭的 `KID_ASSIST`，只能降低决策频率和攻击积极性，不能改变动作规则。
AI只能在1P模式通过阶段3C统一控制接口产生爸爸输入；2P模式必须完全停用AI。禁止AI直接修改爸爸状态，
禁止为2P复制一套战斗逻辑。

池底奖牌按原版视觉与轨迹加入。食人鱼、减耗氧、游速强化等道具放在独立开关中；如果当阶段来不及实现，明确标记未完成，不能用原创替代。

### 阶段5通过条件

- 可以从开场完整玩到结算并重开。
- 时间、HP、Oxy三个胜负链路都能触发。
- AI能换气、下潜、踢击、肩组、骑头和脱身。
- 2P 1V1可以从开场完整玩到结算并重开，双方同时输入不会串键，HUD正确显示 `1P / 2P`。
- 同一输入回放在1P控制源、2P控制源和回放控制源之间保持相同逐tick结果。

---

## 阶段6：原版音画反馈与移动端

### 给 Kimi 的指令

在玩法通过后才补：

- 原版风格BGM循环。
- 水花、踢击、出拳、换气、拉脚、气绝、胜负音效。
- 音效触发帧与参考录像一致。
- 手机FC手柄：十字键、A、B、Start；支持多点触控和按住状态。
- 键盘：方向键，Z/空格=A，X=B，Enter暂停，R重开。

默认不播放家庭彩蛋。只有显式开启 `FAMILY BONUS` 后，原版结算结束才允许展示骑马照和彩带。

### 阶段6通过条件

- 首次用户操作后才启动WebAudio。
- 暂停时逻辑、计时和音乐同步暂停。
- 触屏可以同时按方向和A/B。
- 横竖屏均不遮挡256×224比赛画面。

---

## 阶段7：最终审计

### 给 Kimi 的指令

不要增加新功能，只修验收问题。

逐条执行 `开发规格书.md` 第12节，并额外完成：

- 全文搜索旧竞速变量：`TRACK`、`camX`、`finish`、`distance`、`energy`，结果必须为零。
- 全文搜索禁用绘图API。
- 检查所有资源路径和图片加载失败兜底。
- 连续运行至少三场，检查状态泄漏和无法重开。
- 模拟低帧率，确认固定步进不会改变游戏速度。
- 输出一份 `过程文件/Kimi开发/最终验收记录.md`，每项写“通过/未通过/证据”。

最终只交付：

`/Users/xingbili/Downloads/kimi/阳阳游泳游戏/阳阳水泳大乱斗-原版复刻.html`

未通过项必须如实列出，不能用“基本完成”掩盖。
