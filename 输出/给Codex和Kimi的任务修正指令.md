# 给 Codex 和 Kimi 的任务修正指令

> 在 VSCode 里怎么分别下达。**两边的指令不一样，不要互抄。**
>
> 前置：先让两边都读 `输出/唯一尺寸与画风合同.md`。那份文件是尺寸与画风的唯一权威，
> 它显式作废了规格书、路线草案、各轮验收记录里的一批冲突条款。
> 不先读它，两边会继续引用已作废的条款。

---

## 〇、先看清楚分工为什么要改

过去两周分工塌了：

- **Codex** 在 `CONCEPT-ACTION-0-P0` 里同时是定档说明作者、生成器作者、预审人——**出题答题判卷三合一**。
- **Kimi** 被要求做一件它做不到的事（用代码手绘像真人的角色），只能不断交出"机器全 PASS、视觉不通过"的东西。

新分工：

| 角色 | 只做这些 | 明确不做 |
|---|---|---|
| **Kimi** | 写玩法代码、写图像处理流水线 | **不再"画"人物**——它写的是处理概念图的脚本，不是绘制脚本 |
| **Codex** | 审 diff、审契约、审确定性、审规则一致性 | **不产出美术、不产出生成器**——它产出的东西不能由它自己预审 |
| **你** | 唯一的视觉裁判，只在定稿会上裁一次 | 定稿后进入冻结，后续阶段不重开视觉评审 |

---

## 〇之二、下一轮的执行顺序：Codex → Kimi → Codex

**先 Codex，不是先 Kimi。** 理由有两条：

1. B0（SPRITES 契约）和 B1（比赛流程）刚刚改完主 HTML，**还没有任何人审过**。
   而 B2 是约 2 个工作时段的代码，会直接建在 `SPRITES` 契约和 `MATCH` 之上。
   契约层的设计缺陷现在发现最便宜，等 B2 写完再发现就是返工。
2. 这也让 Codex 用一个具体任务回到"只审不产出"的正确位置。

**但必须给 Codex 一个阻塞门槛，否则它会无限产生异议。**
"60 多条新禁令散落在验收记录里"就是没有门槛的结果。门槛写死为：

> 只有三类问题可以阻塞：**破坏确定性 / 破坏契约 / 游戏跑不起来**。
> 其余一律进"建议"桶，不阻塞下一阶段。

这一轮是个短门（十几分钟）。**如果 Codex 只报出建议级问题，直接进 Kimi 的 B2，不要返修。**

---

## 一、给 Kimi 的指令（写代码）

### 在 VSCode 里怎么下达

Kimi 需要能改文件。开新会话，第一条消息先给它上下文，再给任务。

**第一条消息（每次新会话都要贴，它没有记忆）：**

```
项目：FC《热血游泳大会》复刻，单文件 HTML 游戏。
先读这三个文件，读完再动手：
  1. 输出/唯一尺寸与画风合同.md   ← 尺寸与画风的唯一权威，已作废一批旧条款
  2. 输出/重启规划-下一步怎么做.md ← 为什么要改流程
  3. 阳阳水泳大乱斗-原版复刻.html  ← 游戏本体

三条硬规则，违反即本轮作废：
  A. 你的产出必须进入游戏。只产出静态 PNG 或文档的轮次一律不批。
  B. 不许把新禁令写进验收记录或脚本注释。规则只能改总法（合同文件），
     且必须经我同意。现在验收记录里散落着 60 多条新禁令，是返工的主因。
  C. 不许推翻重做。在上一轮基础上修补。十几轮从空画布重启，累积收益为零。

本轮结束时告诉我：改了哪几个文件、怎么验证的、哪里没做完。
不要写验收记录文档。
```

**然后给具体任务。下一轮（B2 战斗内核）可以直接抄：**

```
任务 B2：同步战斗内核 + 踢击。

现状（已经做好的部分，不要重做、不要推翻）：
- SPRITES 数据契约已在 HTML 里，玩法代码只认契约不认美术，
  frameIdFor(c) 是玩法层与美术层唯一的接触面。
- KICK / HIT / GRAPPLE* / MOUNT* / LEGPULL* 的占位帧已全部存在，
  KICK 与 GRAPPLE_PUNCH 已带 provisional hitbox。你可以立刻引用，不必等美术。
- MATCH / resetMatch / endMatch / resolveMatch 已经能跑：
  3:00 倒计时、HP、气绝掉血、胜负判定、Enter 重开都已工作。
- 横向钳制带由 spriteExtents() 从当前资产推导，不要再写死任何尺寸。

你要做的四件事：

1. 把 tick() 改成快照式结算（规格书 3.1）：
     ① 冻结双方旧状态快照 + 同时读取双方输入
     ② 各自 decide() 生成意图，只读快照，不读对方的新状态
     ③ resolve() 统一解析碰撞/命中/优先级
     ④ commit() 同时提交
   目的是消除先手偏差：现在 tickChar(yy) 先于 tickChar(dad)，是有偏的。
   输入源要抽象成 inputSource，为 1P / 2P / CPU / REPLAY 留统一接口
   （本轮只需实现 1P 与 IDLE，不要写 AI，AI 是 B5）。

2. 建立 interaction 骨架（本轮只搭结构，不实现肩组/骑头/拉脚）：
   唯一一个 interaction 对象驱动成对交互，两个角色都只持有它的 id，字段至少包含
     id / kind / phase / pairTick / aId / bId / aRole / bRole
     / pairAnchor / facing / drawOrder / appliedEvents
   建立、中断、结束必须原子发生；事件用 (interactionId, eventId) 去重。
   本轮 kind 只有 'KICK' 一种命中事件，但结构要按四种交互设计。

3. 实现水下踢击（非肩组状态按 B）：起手 / 有效 / 收招三相位，
   命中 = 扣 HP + 击退 + 硬直 + 切 HIT 帧。

4. 判定盒改成独立数据：写进 SPRITES 帧的 hitbox / hurtbox 字段并按相位给值，
   去掉 provisional 标记。不许用透明像素外接框代替。

通过条件（缺一不可，缺任何一条本轮不算通过）：
- 双击 HTML，阳阳能踢中爸爸并把他的 HP 打掉。
- ?selftest 仍然 PASS。
- 新增一条自检：同一初始状态 + 同一输入流跑两遍，逐 tick 状态 hash 完全一致。
- 占位版和本地美术版都能跑、都能 PASS
  （后者用 `python3 过程文件/Claude重启规划/bake.py --out` 生成）。

如果我先给了你 Codex 的 review 结论，先按结论里标"阻塞"的项修，
标"建议"的项不要动——那些不阻塞本轮。
```

### 给 Kimi 的长期禁令（只有这一条）

```
禁止用 rect / ellipse / polygon / disc 绘制任何人物像素。
人物的每个像素都必须能追溯到概念图的原像素。
需要新姿态时，你写的是"处理概念图的脚本"，不是"绘制人物的脚本"。
（场景、HUD、粒子、字模不受此限。）
```

---

## 二、给 Codex 的指令（审查）

### 在 VSCode 里怎么下达

Codex 只读不写。每次 Kimi 交付后开一个**新**会话，让它审。

**关键：不要让它在同一个会话里既提方案又审自己的方案。**

```
你的角色是代码审查者。本轮只审，不产出代码、不产出美术、不产出生成器脚本。
如果你觉得需要改，写成 review 意见给我，由 Kimi 执行。

审查对象：git diff（或 Kimi 本轮改动的文件）

先读 输出/唯一尺寸与画风合同.md。它是尺寸与画风的唯一权威，
显式作废了规格书 5.1/5.2/5.3/5.5、第7节，概念路线草案第5节，
以及各轮验收记录里的 mainHtmlUnchanged 条款。
**引用已作废条款的 review 意见无效。**

按这五条审，每条给出结论 + 文件:行号证据：

1. 契约一致性
   玩法代码是否只通过 SPRITES 契约访问美术？
   有没有绕过 frameIdFor / drawFrame 直接读 BODY/AVATAR/HEAD_* 的地方？
   （允许的例外：bakeFrames() 内部、SPLASH 粒子。）

2. 确定性
   有没有引入非整数状态、Math.random、Date.now、遍历顺序依赖？
   固定输入流的逐 tick 状态 hash 是否可复现？

3. 同步结算
   成对交互是否由唯一一个 interaction 对象驱动？
   有没有"两个角色各自猜测对方状态"的写法（会产生先手偏差、穿插、永久锁死）？
   建立/中断/结束是否原子发生？

4. 判定盒
   hitbox/hurtbox 是否是独立数据？有没有退化成透明像素外接框？
   跳跃骑乘是否用了"上一tick到下一tick的向下扫掠 + 水平重叠 + 目标可骑状态"
   三者共同判定？（只检测当前是否相交会漏掉高速穿透。）

5. 资产可替换性
   占位版和本地美术版是否都能跑、都能 selftest PASS？
   有没有把某个具体尺寸/锚点写死，导致换美术就崩？
   （已经发生过一次：横向钳制带写死 [20,236]，40px 正式帧一接进来就出画。）

输出格式：每条给「通过 / 不通过 + 证据 + 建议怎么改」。
不要重写代码，不要给完整文件。
```

### 本轮第一件事：审 B0 + B1（在 Kimi 开工之前）

把下面整段贴给 Codex，开一个**新**会话：

```
你的角色是代码审查者。本轮只审，不产出代码、不产出美术、不产出生成器脚本。
需要改动的地方写成 review 意见给我，由 Kimi 执行。

先读 输出/唯一尺寸与画风合同.md。它是尺寸与画风的唯一权威，显式作废了
规格书 5.1/5.2/5.3/5.5 与第7节、概念路线草案第5节、以及各轮验收记录里的
mainHtmlUnchanged 条款。**引用已作废条款的 review 意见无效。**

审查对象：阳阳水泳大乱斗-原版复刻.html 里新增的 B0/B1 两块。
定位符号（行号会变，按符号找）：
  B0 资产层：SPRITES / composeFrame / bakeFrames / drawFrame / uwPalette
             / spriteExtents / ANCHOR_X_MIN_Fp / frameIdFor / drawSwimmers
  B1 比赛层：MATCH / resetMatch / endMatch / resolveMatch / tick
             / tickChar 里新增的 ST.DROWN 与 ST.LOSE 分支
  构建工具：过程文件/Claude重启规划/bake.py

**阻塞门槛（重要）**：只有以下三类可以判"阻塞"：
  (a) 破坏确定性（非整数状态、随机源、遍历顺序依赖、双跑结果不一致）
  (b) 破坏契约（玩法代码绕过 SPRITES 直接读美术数据；换美术会崩）
  (c) 游戏跑不起来或 ?selftest 不 PASS
其余全部归入"建议"，明确标注为不阻塞。不要把偏好写成禁令。

按这六条给结论，每条 = 通过/阻塞/建议 + 文件:行号证据：

1. 契约完整性
   frameIdFor 覆盖了全部 8 个状态吗？有没有可能返回不存在的帧 id
   导致 drawSwimmers 里 f 为 undefined 而抛异常？
   （占位帧和 bake.py 借用帧两种情况都要检查。）

2. 契约隔离
   除 bakeFrames 内部与 SPLASH 粒子外，还有没有别处直接读
   BODY / AVATAR / AVATAR_BREATH / HEAD_SIDE / HEAD_DOWN / LAY？

3. 换美术是否会崩
   还有没有写死的尺寸、锚点或坐标，在正式帧（宽 61px，占位帧宽 38px）下会出画？
   （已经踩过一次：横向钳制带写死 [20,236]，现改为 spriteExtents() 推导。
     请找还有没有同类残留，特别是 UW 纵向域 uwTopFp/uwBotFp 和 HUD 布局。）

4. 比赛流程正确性
   resolveMatch 的判定顺序有无漏洞（双方同时归零、时间到同分）？
   endMatch 后 tickChar 是否已停止推进？重开是否把全部状态清干净
   （boost / swimT / uwPose / jumpDir / parts）？
   气绝 → 掉血 → 呛回水面 这条链有没有可能死锁或无限循环？

5. 确定性
   B0/B1 新增代码有没有引入非整数状态或随机源？
   uwPalette 的缓存（Object.defineProperty 挂 __uw）会不会污染契约自检的键遍历？

6. bake.py 的正确性
   借用帧（ALIAS）是否可能指向不存在的源帧？
   注入/回退（--out / --inject / --restore-placeholder）是否幂等？
   主 HTML 会不会被写进家庭外貌特征像素？

最后给一句总结：**可以进 B2 吗？**（只看是否存在 (a)(b)(c) 类阻塞项。）
```

### 给 Codex 的长期禁令

```
1. 不产出美术资产、不写生成器脚本。你产出的东西不能由你自己预审。
2. 不新增禁令。发现规则有问题，提议修改《唯一尺寸与画风合同》，由我裁决。
3. 不把"主 HTML 未改动"当作通过条件。这条已作废——
   现在的规则相反：产出不进游戏的轮次一律不批。
```

---

## 二之二、拿到 review 之后怎么分流

| Codex 的结论 | 你要做的 |
|---|---|
| 只有建议级 | **直接进下一阶段，不返修。** 建议全部记入 `输出/建议台账.md`，标好归属阶段 |
| 有阻塞项且附了复现步骤 | 只把这几条交给 Kimi 先修，修完再进下一阶段 |
| 有阻塞项但没有复现步骤 | 退回，要求补复现。没有复现路径的阻塞判定一律降级为建议 |
| 交出的是一长串禁令式意见 | 门槛没生效。忽略，把"阻塞门槛 + 阻塞必须可复现"那两段再贴一次 |

**非阻塞的建议只能进台账，不得写进验收记录、阶段说明或脚本注释。**
这是"60 多条新禁令散落在各处"的直接对策。

### B0/B1 轮的实际结果（2026-08-24）

Codex 结论：4 项通过 / 3 项建议 / **0 项阻塞**，明确"可以进 B2"。
三条建议已记入台账（S-01 归 A3、S-02 归 B4、S-03 随手），本轮**不返修**。

### B2 轮的预跑事实（2026-08-25，交给 Codex 时一并给它）

Kimi 交付 B2 后已做过一次独立复跑，以下事实**已经验证过，Codex 不必重复跑，
也不要把这些再当成待查项**：

| 项目 | 实测结果 |
|---|---|
| 占位版 `?selftest` | PASS，`errs: []` |
| 本地美术版 `?selftest` | PASS，`errs: []` |
| 确定性双跑（900 tick 逐 tick state hash） | 真实存在且覆盖 `interaction`，两遍完全一致 |
| 水下踢击 | 能进 `KICK`，命中扣 6 HP，受击方进 `HIT` |
| `interaction` 单例 | 一次完整踢击全程只产生 **1 个** 交互对象，未重复创建 |
| 判定盒左右镜像 | 占位版 164 帧全对称；**美术版 74 帧差 1px**（见台账 S-06） |
| 判定盒覆盖率 | 尺寸不随帧宽缩放，两版覆盖率差约一倍（见台账 S-04） |

最后两项**已判定为建议级、已入台账、本轮不返修**。Codex 不要把它们报成阻塞。

---

## 二之三、以后每一轮怎么走（模板，不用再问人要提示词）

每一轮都是同一个四步循环，换的只是阶段名和任务内容：

```
① 你把下面【给 Kimi 的模板】填好 → 新开 Kimi 会话 → 它写代码
② 它说完成后，你自己跑：node 过程文件/Claude重启规划/verify.js
   退出码非 0 就直接打回，不用开 Codex 会话（连自检都没过，没什么好审的）
③ 退出码 0 → 把下面【给 Codex 的模板】填好 → 新开 Codex 会话 → 它审 diff
④ 按"二之二"的表分流：只有建议就进下一阶段，有阻塞且带复现才返修
```

**关键：② 一定要在 ③ 之前。** 让机器先挡掉硬错误，Codex 才不会把精力浪费在
"这里会不会崩"上，而是去看机器测不出来的东西（隔离性、扩展性、语义正确性）。

### 给 Kimi 的模板

```
参考《输出/唯一尺寸与画风合同.md》和《输出/给Codex和Kimi的任务修正指令.md》，
在 阳阳水泳大乱斗-原版复刻.html 上实现 <阶段号>：<阶段名>。

【三条硬规则】
1. 只改主 HTML。不新建阶段目录，不写验收记录、不写阶段说明。
   拿不准的、想提的建议，写进《输出/建议台账.md》并标好归属阶段。
2. 所有玩法状态用定点整数（FP()），不得引入浮点、Date、Math.random 或
   遍历顺序依赖。新增的玩法状态必须进 hashState()。
3. 不碰 SPRITES 契约的结构，不碰 bake.py。需要新帧就用现有帧 id，
   缺帧走占位，不要为了这一轮去改美术管线。

【现状】
<用一句话说清上一阶段做完了什么，比如：B2 已完成，tick 是快照式结算，
 interaction 骨架已就位，水下踢击可用>

【本轮任务】
<逐条列，每条都要能被 verify.js 或 ?selftest 验证>

【通过条件】
1. node 过程文件/Claude重启规划/verify.js 退出码为 0
2. <本轮特有的、可机器验证的条件>
```

### 给 Codex 的模板

```
审查范围：主 HTML 的 <阶段号> 改动。只依据《输出/唯一尺寸与画风合同.md》
和下面的预跑事实判断，不要引用任何历史轮次的验收记录或禁令。

【已验证的事实，不要重复跑，也不要当成待查项】
<把 verify.js 的输出整段贴进来>
其中"观察"项已记入《输出/建议台账.md》，本轮不返修，不要报成阻塞。

【审查项，只审这五条】
1. 隔离性：<本轮新增逻辑>有没有读到不该读的状态？
2. 生命周期：本轮新建的对象/状态，是否一定会被清理？有没有泄漏或悬空引用？
3. 语义正确性：<本轮特有的规则>在边界情况下是否仍然成立？
4. 确定性：新增代码有没有非整数状态、遍历顺序依赖、随机源？
   hashState() 有没有漏掉影响玩法的状态？
5. 下一阶段可扩展性：<下一阶段要做的事>能不能挂进现在的结构而不推翻它？
   不能的话，指出具体哪个字段挡路。

【阻塞门槛】只有三类算阻塞，其余一律写成"建议"并标归属阶段：
(a) 破坏确定性　(b) 破坏 SPRITES 契约或换美术会崩　(c) 跑不起来 / selftest FAIL
每条阻塞必须附可复现步骤。没有复现步骤的，自行降级为建议。

【输出格式】逐条给"通过 / 建议 / 阻塞"，最后一行明确回答：能不能进 <下一阶段号>。
```

### 剩下的阶段和各自的"通过条件"

| 阶段 | 内容 | 机器可验证的通过条件 |
|---|---|---|
| **B3** | 肩组 / 骑头 / 拉脚三种成对交互 | 三种交互各能触发一次、各自只产生 1 个 interaction、结束后必被清空 |
| **B4** | 气绝演出 / 败北演出 / 结算画面 | 时间到判负时败者进 `LOSE`（台账 S-02 在这轮落地） |
| **B5** | 输入源接入：2P / CPU / REPLAY | 同一 replay 流两遍跑出的逐 tick hash 完全一致 |
| **A3** | 32 个动作正式帧补齐、占位清零 | 占位帧数为 0；台账 S-01 / S-04 / S-06 在这轮一起落地 |

**A3 要等 B3/B4 做完再做。** 理由：占位帧现在是 KICK/GRAPPLE/MOUNT 唯一可用的资产，
提前清掉会让 B3 无帧可用。美术永远跟在玩法后面走，这是这次重启的核心决定之一。

---

## 三、三条止损信号（出现任何一条就停下来）

1. **某一轮的产出没有进入游戏** → 立刻停。产物不进游戏的阶段一律不批。
2. **验收记录里出现新的禁令** → 立刻停。规则只能改总法，不能写在验收附注里。
3. **同一个东西被推翻重做第二次** → 立刻停，改成"在上一轮基础上修补"。

---

## 四、常用命令备忘

```bash
# 从概念图生成 40px 精灵（确定性，可重复跑）
python3 过程文件/Claude重启规划/build_sprites_from_concept.py --target-height 40 --verify

# 生成本地美术版 HTML（主 HTML 保持占位资产，始终可安全提交）
python3 过程文件/Claude重启规划/bake.py --out

# ★ 每轮验收就跑这一条。退出码 0 才能进下一阶段
node 过程文件/Claude重启规划/verify.js

# 单看内置自检（浏览器打开，看标题栏是 PASS 还是 FAIL）
open "阳阳水泳大乱斗-原版复刻.html?selftest"
```

`verify.js` 把两版 HTML 的自检、确定性双跑、踢击命中、判定盒镜像与覆盖率
一次跑完，分【阻塞】和【观察】两类：**只有阻塞项挡路，观察项永远不影响退出码**。
这是"审查意见变成新禁令"的机器化对策——观察项的去处是台账，不是下一轮的任务。

**隐私**：`sprites/`、决策图、`阳阳水泳大乱斗-本地美术勿提交.html` 都含家庭外貌特征，
已全部写进 `.gitignore`。主 HTML 用占位资产，可以安全提交。
