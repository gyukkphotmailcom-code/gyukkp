# 《阳阳 VS 爸爸 · 水泳バトルロイヤル》原版高还原开发规格书

> 把这份文档整份丢给 Kimi，让它照做。**这不是"参考建议"，是验收标准。**

---

## 0. 一句话任务

高还原复刻 FC《びっくり熱血新記録！はるかなる金メダル》（中文《热血新纪录》，美版 *Crash 'n' the Boys: Street Challenge*）里的第三个项目「**水泳バトルロイヤル**（游泳大乱斗）」。除阳阳、爸爸的头像和适配网页所必需的输入层外，规则、操作状态、场景构图、动画节奏、判定、音效反馈和单场流程均以 FC 原版为准。

头像不是把照片直接贴进游戏，而是根据照片**人工归纳特征后重绘成 24×24 FC 像素头像**：保持阳阳的泳帽/泳镜/儿童脸型，保持爸爸的眼镜/发型/成年脸型，让两人一眼可辨，同时与原版身体、描边和调色板统一。

**输出**：`/Users/xingbili/Downloads/kimi/阳阳游泳游戏/阳阳水泳大乱斗-原版复刻.html`
单个 HTML 文件，零外部依赖，双击浏览器直接能玩。三张照片已经放在同目录。

---

## 1. 先纠正一个根本性误解（上一版就是栽在这里）

上一版做成了**100 米竞速赛**：横向卷轴、终点墙、"距离 47/100m"、领先落后进度条，打架只是竞速中的骚扰手段。

**原作里这些一个都没有。**

原作是**泳池里的 1v1 对战**：没有终点线，没有距离。玩家通过水下踢击、贴身肩组出拳、骑头压制、从水下拉脚等方式消耗对方 HP 和空气。

所以：
- ❌ 删掉：`TRACK`、`camX` 横向卷轴、终点墙、距离显示、领先/落后、进度条、必杀槽
- ✅ 加上：**体力(HP)槽**、**比赛倒计时**、**跳跃骑头**、**缺氧气绝掉血**、水下八向移动、踢击、肩组出拳、拉脚下潜

### 1.1 原版证据优先级（新增硬性要求）

出现冲突时按以下优先级处理，不允许凭感觉补规则：

1. FC 日版实机逐帧录像与输入回放
2. 任天堂官方电子说明书与官方操作页面
3. 同版本实机截图
4. 攻略资料
5. 本规格书中的暂定数值

所有未通过逐帧测量确认的速度、伤害、判定框和倒计时，都必须集中放入 `ORIGINAL_TUNING` 常量表并标注 `待标定`，不得宣称为原版精确值。

### 1.2 动作证据与时序精度（硬性要求）

- 每个需要标定的动作必须取得同版本原版的连续帧；单张官方截图只能证明静态姿态，不能证明起止、位移、有效帧或循环节奏。
- 数值标定应寻找至少 3 次无接触污染、角色可追踪的有效实例；不足 3 次时必须保留 `待标定`，不得用平均猜测补齐。
- 30fps 编码录像只能把 60Hz 逻辑时序约束到约 2 tick 粒度。只有取得 60Hz 逐帧录像或输入回放时，才允许宣称误差不超过 1 tick；仅有 30fps 证据时必须明确标注约 `±2 tick` 的不确定度。
- 跳跃、攻击、受击和抓取必须保留动作前后至少 1 秒的上下文，用来排除骑乘、肩组、碰撞或其他角色造成的混合动作。
- 原版证据缺失的姿态可以作为带标签的临时占位稿，但不得写成“原版已还原”，也不得通过对应阶段的视觉验收。
- 建立 `EVIDENCE_LEDGER`，每个参数反向关联 `ORIGINAL_TUNING` 键，并记录 `version/source/sourceFps/sampleId/startFrame/endFrame/tickRange/anchors/gaugeDelta/notes`。日版、美版以及不同角色能力模板的数值不得混为同一精确常量。

如果做完之后画面上还能看到"米数"或"进度条"，就是没读这一节。

---

## 2. 原作规则（必须严格照做）

| 项目 | 设定 |
|---|---|
| 场地 | 一个固定的泳池，**不滚屏**，两人始终同屏 |
| 时间 | 单场 **3:00**；公开原生 256×224 实机录像已确认从 3:00 开始倒数 |
| 两条槽 | **体力 HP**（主）+ **氧气 AIR**（副），两人各一套 |
| 胜负 | 把对方 HP 打到 0 → 败北；时间到则 **HP 高者胜**；平局处理按实机验证，不自行增加突然死亡 |
| 水面 | B = **大口呼吸**（氧气快速回满）；A = **跳跃**，落到对方头上把他按进水里 |
| 水下 | 方向键八向移动；移动中配合 A 连续操作可提高速度；B = **踢击** |
| 肩组 | 水下朝对手方向移动并接触后进入肩组；肩组状态按 B = **出拳** |
| 拉脚 | 位于水面敌人下方时 **↑+B**，把对方从水面拉入水中并消耗其空气 |
| 骑乘脱身 | 被骑时按住 **↓** 潜水脱身；不得设计“↓+B 连打百分比挣脱槽” |
| 氧气 | 潜水时持续消耗；**挨打会额外大量掉气**；浮出水面自动恢复，按 B 恢复更快 |
| 缺氧 | 空气归零后进入原版的**气绝/昏迷状态**，暂时失去正常行动能力，期间 HP 持续减少；恢复条件和持续时间按实机标定 |
| 赛制 | 一场定胜负；本项目不增加三局两胜、回合灯或 15 秒突然死亡 |
| 池底奖牌 | 按原版时机、轨迹与拾取判定生成；奖牌是促使玩家下潜的重要风险收益机制 |
| 角色差异 | 阳阳、爸爸分别映射到一套原版角色能力模板；游速、攻击、防御不能被写死为完全相同 |
| 赛前道具 | 原版模式提供原版游泳相关强化开关，包括减缓耗氧、提高游速及食人鱼干扰；名称和效果以日版实机为准 |

**氧气归零不是立刻判负，而是进入气绝并持续掉血。**不要把它改成仍可半速自由移动、主动攻击或使用冲刺的普通状态。

---

## 3. 技术底座

```
逻辑分辨率：256 × 224（NES 原生）
显示：整数倍放大 ×3 = 768 × 672，canvas 用 image-rendering: pixelated
        ctx.imageSmoothingEnabled = false
逻辑帧率：固定 60fps，用累加器 (accumulator) 步进，渲染与逻辑分离
坐标：世界状态可用整数定点数；进入绘制前统一量化为整数像素，绝不允许半像素
```

**硬性禁令**（违反任何一条都判不合格）：
- ❌ `createLinearGradient` / `createRadialGradient`——渐变一律用**抖动网点 dither** 实现
- ❌ `roundRect` / 任何圆角
- ❌ `globalAlpha` 做半透明——半透明一律用 **50% 棋盘格网点**
- ❌ `ctx.ellipse` 画角色身体——角色必须是逐像素画出来的精灵
- ❌ 系统抗锯齿字体——必须用自绘的 **8×8 像素字模**（数字 0-9、A-Z、以及需要的汉字用 12×12 点阵）
- ❌ 任何 CSS `box-shadow` / `border-radius` / `blur`

### 3.1 双人状态必须同时结算

战斗、碰撞和双人交互不得采用“先完整更新阳阳，再用新状态更新爸爸”的顺序。每个固定 tick 必须使用同一份旧状态快照，并按以下四段执行：

```text
读取双方同一 tick 的输入与旧状态快照
→ 双方分别生成移动/攻击意图
→ 统一解析碰撞、命中、抓取和状态优先级
→ 同时提交事件、数值、位置与新状态
```

肩组、拉脚和骑乘必须由唯一的双人 `interaction` 对象驱动，保存双方角色、共同锚点、相对偏移、阶段、绘制顺序和已经触发的事件。禁止让两名角色各自猜测对方状态，否则会产生先手偏差、穿插和永久锁死。

`interaction` 至少包含 `id/kind/phase/pairTick/aId/bId/aRole/bRole/pairAnchor/facing/appliedEvents`。每名角色同时最多参与一个交互，双方保存的ID和职责必须互相匹配；建立、中断与结束必须原子发生。伤害、耗氧和释放等marker用 `(interactionId,eventId)` 去重。相同初始状态和输入流必须产生完全相同的逐tick状态hash。

---

## 4. 原版场景布局（已依据 256×224 实机帧修正）

旧规格把 HUD 放在顶部、把水面写成 `y=96`、把池底写成 `y=200`，与原版画面不符，全部废止。当前基准如下：

```js
const SCREEN_W = 256;
const SCREEN_H = 224;
const PLAY_Y = 0;
const PLAY_H = 168;       // y = 0..167
const HUD_Y = 168;
const HUD_H = 56;         // y = 168..223

const BG_BOTTOM = 71;     // 远景/看台/跳台区域约 y = 0..71
const SURFACE_TOP = 72;   // 透视水面约 y = 72..101
const UNDERWATER_Y = 102; // 水下区域约 y = 102..165
const PLAY_DIVIDER_Y = 166;
```

坐标来自原生 256×224 实机录像；视频是30fps编码，布局坐标可信，1帧动作时序仍需60Hz输入录像复核。

### 4.1 日版视觉基准

- 上半场景以日版官方截图为准：城市建筑、富士山、泳池两侧建筑/旗帜，不使用美版自由女神像和美式城市布景。
- 水面是强透视的白/浅蓝块状波纹，占约30px高度，不是单条水平线。
- 水下是透视池体：左右墙向中央收束、池底线向远方消失；不能画成正交矩形瓷砖背景。
- 中央泳道绳贯穿水面和水下，水面部分为黑白交替浮球，水下部分为青色透视分段线。
- 场景使用硬切色块和有限色调色板，没有景深渐变、半透明水层或全屏蓝色透明罩。
- 水下角色直接切换为青/蓝水下专用精灵配色，不使用 `globalAlpha` 或棋盘格覆盖整个人物。

### 4.2 HUD 精确结构

- HUD 固定在底部 `y=168..223`，黑底白色方框；比赛场景绝不覆盖 HUD。
- 第一行：左侧 `1P + 名字`，中间 `TIME`，右侧 `CPU/2P + 名字`。
- 第二行：左右各一条 HP，计时数字位于中间；初始时间显示 `3:00`。
- 第三行：左右各一条 `Oxy` 空气槽。
- 1P和CPU的条均从左向右减少，不使用旧规格中的“爸爸从右向左镜像填充”。
- 原版 HUD 使用8×8英文字模与数字字模。定制姓名使用同字宽的像素字形，不加圆角面板、图标或回合灯。

---

## 5. 角色规格

### 5.1 动作精灵尺寸与结构（以原版轮廓为准）

- 阶段2通过的 24×24 头像是**身份与表情母版**，可用于头像预览、HUD或提取泳帽、泳镜、眼镜、发型和五官特征；它不是运行时必须整块粘贴的固定头部 socket。
- 每个动作使用按原版帧测得的可变尺寸完整人物精灵。头部朝向、躯干、双臂和双腿必须在同一动作轮廓中设计，禁止继续使用“完整24×24头先贴上，再让16×16身体填空”的机械拼接方式。
- 阳阳和爸爸应分别输出完整动作精灵；除脸部、泳帽/发型、泳镜/眼镜及辅助队色外，身体比例、泳衣、肢体姿态和动作轮廓以同一原版角色模板为准。
- 头像替换只允许作用于该动作头部内部的身份特征区，不能改变原版动作所需的头部朝向、颈肩连接和整个人体外轮廓。
- 精灵画布、身体外接框和透明边距按动作分别记录。所有动作使用稳定的世界锚点；直立动作通常用脚底中心，横游、抓取和骑乘另记录身体中心或双方共同接触锚点。
- 碰撞盒、受击盒、攻击盒和抓取点是独立数据，不能直接用透明像素外接框代替；具体尺寸与偏移必须来自原版证据并写入 `ORIGINAL_TUNING`/动作定义。

#### 人体优先的两层美术门

“像阳阳/爸爸”的前提是先像一个完整的人。每个代表动作必须先制作不含任何定制身份像素的
**原版人体母版**，通过原生1×人工盲审后，才允许添加身份层：

1. 人体母版由锁定的原版原生裁剪、逐像素确认的 subject mask 和版本固定的语义调色分类器
   生成。禁止用手写矩形/大色块重画身体，禁止全局填洞，禁止拿头像像素重复铺满动作头部。
2. 人体母版的 alpha 必须与 subject mask 完全相等；内部每个像素必须能反向追溯到原版裁剪
   和分类规则。轮廓XOR通过不能替代内部泳衣、肢体、五官、负空间和头身接口的验证。
3. 身份层必须使用每个动作单独批准的唯一头部内部 mask。所有改动满足
   `changedPixels ⊆ approvedIdentityMask`；mask外相对人体母版的RGBA、调色类别和alpha差异均为0。
4. 阶段2的24×24头像只能作为辨识特征参考/母版；动作内的泳帽、泳镜、发型、眼镜和五官
   必须按该动作头部尺寸重新逐像素适配，不得整块粘贴、缩放或横向重复头像行。
5. 自动测试只能证明像素合同；“原生1×下是否一眼读作一个完整的人和动作”必须由用户/Codex
   人工放行。人体不通过时，身份辨识再强也不得进入下一阶段。

### 5.2 基于照片重绘 FC 像素头像（**替代照片缩小贴图方案**）

不能把照片直接缩到 24×24，也不能使用圆形照片遮罩。直接缩图即使做 posterize，仍会产生五官噪点和“照片贴纸”感。正确方式是：照片只作为辨识特征参考，最终头像是手工可控的像素精灵。

#### 阳阳头像必须保留的识别特征

- 儿童圆脸和较短的下巴
- 泳帽外轮廓
- 泳镜或泳镜带的高对比形状
- 明亮、活泼的表情
- 队色使用黄色/蓝色，但肤色与原版身体共用同一套肤色色阶

#### 爸爸头像必须保留的识别特征

- 成年人较长、较宽的脸部轮廓
- 眼镜的镜框与镜腿，至少在正面和侧面都能读出来
- 发际线、发型或眉形中的一项稳定特征
- 微笑表情
- 队色使用橙色/红色，但不能只靠颜色区分身份

#### 制作流程

1. 从照片裁剪框提取脸部参考，但不直接用于游戏渲染。
2. 先画 24×24 身份母版，只使用 1px 像素笔、无抗锯齿；该母版不等于每个动作都要复用同一整颗头。
3. 每个头像最多使用：透明色 + 轮廓色 + 3 档肤色 + 2 档头发/泳帽色 + 1 个强调色。
4. 五官按原版 Q 版比例重新安排，不追求照片级写实；优先保证 3 倍放大后能一眼认人。
5. 为左右朝向分别绘制或人工修正。爸爸的眼镜、阳阳泳镜不能简单镜像后出现错误高光。
6. 为正常、挨打、气绝、KO 至少提供 4 个表情变体；身体动作仍使用原版比例的像素精灵。
7. 头像身份特征最终烘焙进各动作的完整人物精灵；若保留独立头像数据，只能用于预览/HUD或生成期合成，运行时仍按整数坐标 1:1 绘制已经验收的完整动作帧。

阵营色只能作为辅助：阳阳使用黄色硬边提示，爸爸使用橙色硬边提示。头像本体先加 1px 深色外轮廓，再在必要位置使用 1px 队色点缀，避免整颗头被彩色圆环包住。

**验收**：隐藏姓名和 HP 条后，让不知情的人只看两个 72×72 放大头像，仍能稳定指出谁是阳阳、谁是爸爸；缩回 24×24 后不能出现摄影纹理、半透明边缘和抗锯齿。

### 5.3 照片裁剪坐标（我已经逐张验证过，直接用，别自己猜）

```js
// 17296.jpeg  原图 1280 × 1707（阳阳戴泳帽泳镜，正好是游泳主题）
const CROP_YY  = { sx: 530,  sy: 480, sw: 340,  sh: 340  };

// IMG_0387.jpeg 原图 2316 × 3088（爸爸戴眼镜微笑）
const CROP_DAD = { sx: 1214, sy: 880, sw: 1100, sh: 1100 };

// IMG_0806.jpeg 原图 4284 × 5712（阳阳骑马大笑）→ 只在胜利结算画面用
```

注意两个裁剪框都是**正方形**，别再用上一版那种 986×1450 的长条框（会导致 `Math.min(sw,sh)` 缩放后纵向溢出、脸偏上）。

### 5.4 动画帧（每个角色都要有）

下表的帧数是最低动作相位，不代表已测得的最终原版帧数。最终切帧、重复和持续时间必须以连续录像为准。

| 状态 | 帧数 | 说明 |
|---|---|---|
| 水面漂浮 | 2 帧 | 上下浮动 1px，手臂划水 |
| 水下游动 | 2 帧以上 | 普通方向移动，节奏按原版 |
| 加速游动 | 2 帧以上 | 移动中配合 A 连续操作后的加速状态，不做技能式冲刺 |
| 水下踢击 | 2 帧以上 | 非肩组状态按 B |
| 肩组出拳 | 2 帧以上 | 与对手肩组后按 B，和普通踢击使用不同判定 |
| 跳跃 | 3 帧 | 起跳蹲 / 滞空展开 / 下落 |
| 挨打 | 帧数待标定 | 身体后仰；闪烁、硬直与击退帧数按原版测量，不预设白闪2帧 |
| 肩组/肩组挣扎 | 2 帧以上 | 攻方和守方分别绘制，共用双人接触锚点，不得各自独立漂移 |
| 骑乘/被骑挣扎 | 2 帧以上 | 与肩组使用不同的成对动画；被骑者按原版下潜过程脱身 |
| 拉脚/被拉 | 2 帧以上 | 拉脚者与被拉者同步绘制，包含抓住、拖拽、释放相位 |
| 呼吸 | 2 帧 | 嘴张大，头顶冒气泡 |
| 气绝 | 帧数待标定 | 缺氧后的失控动作、表情和气泡按原版 |
| 败北 | 帧数待标定 | HP归零后的姿态和移动按原版 |

### 5.5 动作逐帧复刻与双人编舞（新增硬门槛）

人物动作不是“能读懂就算通过”。除定制脸部身份区外，跳跃、踢击、受击、肩组、挣扎、拉脚、骑乘、气绝和败北都必须尽量复现原版的外轮廓、相位、位移和接触关系。

每个动作必须建立动作证据表并标出：

```text
startup（准备）
contact（首次接触，若有）
active（有效/持续作用）
follow-through（动作跟随）
recovery 或 release（恢复/释放）
```

每帧至少记录：来源帧、人物世界锚点、精灵名、位移、绘制层级、水面遮挡 mask；战斗帧另记录 hitbox、hurtbox、attach 点和事件。未测得的数据集中标记 `待标定`，禁止把想象帧伪装成原版帧。

肩组、拉脚和骑乘属于**双人同步 clip**。每个逻辑帧同时定义双方精灵、共同接触锚点、双方相对 `dx/dy`、朝向、前后遮挡和释放事件。不得把两个独立单人动画临时对齐后当作抓取或挣扎动画。

任何动作在接入主程序前，必须先交付原版与新稿同尺度的：

- 原生 1× 对照；
- 8× 最近邻放大图；
- 排除定制脸区后的轮廓叠加/XOR图；
- 左右方向预览；
- 双人动作的同步 GIF/帧条与接触点表。

建议验收口径：先执行5.1的人体母版/身份层两层硬门；在此基础上，原生清晰参考下，
排除定制身份区后的身体关键边缘、手脚和头身接口误差不超过1px。轮廓
`XOR/union <=10%` 只可作为辅助统计，不能覆盖内部像素错误；身份区外相对已批准人体母版的
RGBA/调色类别/alpha差异必须为0。压缩录像参考的几何误差可放宽到2px，但必须标记为
`provisional`，不得宣称ROM逐像素真值。双人接触点与相对偏移误差不超过1px，全段不得
抖动、穿插或脱离。只有30fps证据时，时序验收按约 `±2 tick` 记录，不伪称1 tick精确。

### 5.6 动画与判定分离

动画帧只负责画面，不能用透明像素外接框直接充当攻击判定。攻击定义至少包含 `startup/active/recovery`、逐 tick 判定框、单次命中去重、伤害/耗氧、击退、硬直和命中停顿。双人交互定义至少包含双方角色、角色职责、共同锚点、相对偏移、阶段、释放/中断条件和事件去重。

跳跃骑乘必须使用上一tick到下一tick的向下扫掠、水平重叠和目标可骑状态共同判定，不能只检测两张透明外接框当前是否相交。全局中断优先级必须集中定义并按原版证据标定；缺证据的优先关系写 `待标定`，不得用原创自动超时悄悄改变玩法。

---

## 6. 操作（2 键 + 方向，严格对应原作 A/B 手柄）

### 键盘
```
方向     ← → ↑ ↓      八向移动 / 组合键
A 键     Z 或 空格     跳跃(水面) / 加速游泳(水下)
B 键     X             呼吸(水面) / 踢击(水下) / 肩组出拳
暂停     Enter
```

### 1P / 2P 1v1 模式与键位

- `1P`：阳阳由玩家操作，爸爸由 CPU 操作；保留现有方向键 + `Z/Space`（A）+ `X`（B）。
- `2P 1V1`：阳阳和爸爸均由真人在同一键盘操作，不运行爸爸 AI。默认键位为：

| 角色 | 方向 | A | B |
|---|---|---|---|
| 1P 阳阳 | `W/A/S/D` | `F` | `G` |
| 2P 爸爸 | `←/→/↑/↓` | `K` | `L` |

键位必须集中保存在可修改的 `CONTROL_BINDINGS`，不得散落在事件处理代码中。浏览器按 `KeyboardEvent.code`
读取按键；每个固定 tick 开始时先分别冻结两名角色的输入快照，再进入双方同步结算。控制来源必须抽象为
`P1 / P2 / CPU / REPLAY`，角色状态机和战斗规则只消费统一的动作输入，不得出现“阳阳专用规则”或
“爸爸 AI 直接改角色状态”。同一动作在 1P 与 2P 模式下的移动、判定、伤害、耗氧和硬直必须完全一致。
考虑普通键盘的多键冲突，验收时至少测试双方同时按住方向键并分别触发 A/B；如具体键盘发生 ghosting，
允许以后通过配置改键，但不得改变动作规则。

### 触屏（手机上玩）
画一个 **FC 手柄样式** 的虚拟控制器：左下角十字方向键（像素风、方形），右下角两个圆钮 A / B，标注红色 A、B。不要画成上一版那种一排白色圆角矩形按钮。

### 完整动作表

| 情境 | 输入 | 效果 |
|---|---|---|
| 在水面 | B | **超级换气**：一次补充大量 AIR；按键响应和动作锁定时长按原版逐帧标定 |
| 在水面 | A | **跳跃**；起跳前用 ←/→ 选择方向，轨迹、滞空和落点按原版标定；落到对手上方进入骑乘 |
| 在水面 | ↓（按住） | 下潜；被骑时同样通过下潜摆脱骑乘 |
| 在水面 | ←/→ | 调整位置和跳跃方向；是否允许持续平移以实机为准 |
| 在水下 | 方向键 | 八向游动；移动速度受角色能力和强化道具影响 |
| 在水下移动 | A 连续操作 | **移动速度提升**；不是20帧冲刺技能，无独立冷却和固定耗气6 |
| 在水下、未肩组 | B | **踢击**；攻击框、有效帧、伤害和击退按原版标定 |
| 水下接触对手 | 朝对手方向移动 | 自动进入**肩组**状态 |
| 肩组状态 | B | **出拳**；肩组胜负与双方状态/输入关系按原版实机标定 |
| 位于水面敌人下方 | ↑ + B | 拉住脚并把对方拖入水中，同时减少其 AIR |
| 被骑 | ↓（按住） | 潜入水下逃脱；不显示原创挣脱百分比槽 |
| 骑在对方头上 | 自动持续 | 持续消耗对方 AIR，直到对方潜水脱身或骑乘因原版条件结束 |

---

## 7. 数值与判定标定（原版模式不允许凭感觉调数值）

禁止继续使用旧规格中的 `HP_MAX=48`、`AIR_MAX=64`、固定冲刺耗气、90帧抓取、15秒突然死亡等未验证数值。建立唯一参数表：

```js
const ORIGINAL_TUNING = {
  matchDisplaySeconds: 180,  // 已确认初始显示 3:00
  matchTicks: 10800,         // 以60逻辑帧暂定；最终按NTSC计时复核
  hpGaugeUnits: null,
  airGaugeUnits: null,
  underwaterAirDrain: null,
  surfaceAirRecovery: null,
  superBreathRecovery: null,
  kickStartupTicks: null,
  kickActiveStart: null,
  kickActiveEnd: null,
  kickRecoveryTicks: null,
  kickHitboxesByTick: null,
  kickHurtboxesByTick: null,
  kickDamage: null,
  kickKnockback: null,
  kickHitstunTicks: null,
  kickHitstopTicks: null,
  grapplePunchDamage: null,
  grapplePairOffsetsByTick: null,
  grappleReleaseRules: null,
  mountedAirDrain: null,
  mountPairOffsetsByTick: null,
  mountReleaseRules: null,
  legPullAirDamage: null,
  legPullPairOffsetsByTick: null,
  legPullReleaseRules: null,
  oxygenZeroStunTicks: null,
  oxygenZeroHpDrain: null
};
```

每个 `null` 必须通过同一版本实机录像至少测量 3 次，记录“开始帧、结束帧、槽变化像素、误差范围”。完成标定后才能进入“原版模式”验收。若希望照顾 6 岁儿童，可另设 `KID_ASSIST`，但默认关闭，且不得污染原版参数。

---

## 8. 爸爸的 AI（原版模式与儿童辅助严格分离）

`ORIGINAL` 模式：依据原版 CPU 录像复现状态转换、反应间隔、换气阈值、跳跃/骑乘偏好、肩组输入频率和随机行为。不得加入固定15帧延迟、20%打空、90帧强制呼吸等人为破绽。

`KID_ASSIST` 模式：作为额外选项，可以降低爸爸的输入频率和攻击积极性，但标题必须明确显示“儿童辅助”，默认仍进入 `ORIGINAL`。两套 AI 共用动作规则，只允许改变决策参数，不允许儿童模式拥有原版不存在的动作。

AI 只在 `1P` 模式产生爸爸的统一输入快照；进入 `2P 1V1` 后必须完全停用 AI，由 2P 键位提供爸爸输入。
两种模式共用同一角色状态机、同步战斗内核和胜负流程，禁止复制出一套“2P专用战斗代码”。

---

## 9. HUD（复刻原版底部信息框）

```
┌──────────────────────────────────────┐
│ 1P  阳阳       TIME   CPU/2P  爸爸   │
│ HP ████████     3:00     HP ████████ │
│ Oxy ████████             Oxy ████████│
└──────────────────────────────────────┘
```

- HUD 黑底、白框、白字，槽体采用原版浅蓝色；不要擅自改成红色 HP、低血闪烁或全红 AIR 警报。
- HP 与 Oxy 的起点、宽度、减少方向、文字基线必须直接对齐参考帧。
- 中间倒计时初始显示 `3:00`；字体、冒号间距与数字跳变按原版。
- 右侧身份标签随模式切换：`1P` 显示 `CPU`，`2P 1V1` 显示 `2P`；只改标签，不改变原版 HUD 的框线、坐标、槽体或姓名布局。
- 日版最终姓名显示可用自绘 8×8/8×16 汉字字模；如果为了忠实 HUD 选择英文，则使用 `YANGYANG` / `DAD` 并遵循原版字宽。

---

## 10. 游戏流程

1. **标题画面**：保留阳阳 VS 爸爸的定制入口；先选择 `1P / 2P 1V1`。选择 `1P` 后再提供 `ORIGINAL / 儿童辅助` AI 切换；`2P 1V1` 不显示也不运行 AI 难度。标题属于定制外壳，不冒充原版赛事标题。
2. **入场**：角色站位、入水方式、开始提示和开始音效按原版录像逐帧复现；不得自行添加 `READY? / FIGHT!`，除非原版确有对应演出。
3. **对战**：单场制，原版制限时间；无回合灯、无三局两胜。
4. **败北演出**：气绝、HP归零、上浮/倒下、胜者动作、文字和音效按原版标定；不得先固定“白闪3帧、hitstop 40帧、慢动作”等未经验证的演出。
5. **原版结算**：先完整播放原版 `WINNER`、胜者动作、计时停止和返回流程，只替换胜者姓名与头像，不显示骑马照、彩带或原创鼓励语。
6. **家庭彩蛋（默认关闭）**：只有标题中显式开启 `FAMILY BONUS` 后，原版结算播放完毕才可追加 IMG_0806.jpeg 像素化骑马照和彩带；它不能插入或替代原版演出。
7. 任意时刻按 R 重开。

---

## 11. 音频（手写 8-bit，模拟 NES APU 四声道）

用 WebAudio 搭一个简易 APU：**方波1（主旋律）+ 方波2（和声）+ 三角波（贝斯）+ 噪声（鼓/水声）**。

- **BGM**：以原版水泳バトルロイヤル曲目的旋律、速度、循环点和 NES 声部分配为参考重新编程；不增加未经原版确认的“最后10秒整体加速1.25倍”。若公开发布，需另备一首同风格原创替代曲。
- **音效**：入水噗通（噪声低通衰减）、出拳「咚」（方波 150Hz 短音 + 噪声）、踩头「砰」（更重）、气泡（正弦短促随机音高）、大口呼吸（噪声上升）、AIR 警报（方波双音重复）、KO（下滑音 + 噪声爆）、哨声、观众欢呼（长噪声 + 上行琶音）。

上一版的音频底层函数（`beep` / `noiseBurst`）可以复用，但每个音效的频率、包络、长度和触发帧必须重新按原版校准；不能把上一版的成品音效直接视为原版音效。

---

## 12. 验收清单（做完逐条对着勾）

**规则正确性**
- [ ] 画面上**没有**任何"米数""距离""进度条""终点"
- [ ] 画面**不横向滚动**，两人始终同屏
- [ ] 有 HP 条，且 HP 是主要胜负判据
- [ ] 制限时间和倒数速度已经按原版实机标定，时间到按 HP 判胜
- [ ] 氧气归零后进入原版气绝状态并持续掉血，不是立刻判负，也不是半速自由移动
- [ ] 水面按 B 执行原版超级换气，恢复量和动作锁定已经标定
- [ ] 水面按 A 能跳跃，能踩到对方头上把他按下水
- [ ] 水下普通 B 是踢击，不是统一出拳
- [ ] 朝水下对手移动接触可进入肩组，肩组中 B 才是出拳
- [ ] 仅在水面敌人下方使用 ↑+B 才能拉脚下水
- [ ] 被骑时按住 ↓ 潜水脱身，没有原创挣脱百分比槽
- [ ] 单场定胜负，没有三局两胜和突然死亡
- [ ] `1P` 下阳阳读取玩家输入、爸爸只通过 CPU 输入源操作，没有 AI 直接改写角色状态
- [ ] `2P 1V1` 下两套键位可同时操作双方，爸爸 AI 完全停用，HUD 右侧显示 `2P`
- [ ] 1P与2P共用同一状态机、攻击判定、数值和胜负流程，没有复制的双人专用规则

**美术**
- [ ] 逻辑分辨率 256×224，整数倍放大，`pixelated`
- [ ] 全文搜索确认：**没有** `createLinearGradient`、`roundRect`、`globalAlpha`（半透明用网点）
- [ ] 角色是逐像素精灵，不是 `ellipse` 画的
- [ ] 照片只作参考，头像已经人工重绘为24×24 FC像素头像，不是缩小照片或圆形贴图
- [ ] 隐藏名字和队色后仍能只靠泳帽/泳镜、眼镜、脸型等特征分清阳阳和爸爸
- [ ] 动作精灵是按原版整体轮廓绘制，不是完整24×24头像机械拼接16×16小身体
- [ ] 水下使用原版青/蓝专用精灵色，不使用透明蓝罩或自创深度渐变
- [ ] 每个角色至少有普通游泳/加速游泳/踢击/肩组出拳/跳跃/骑乘/挨打/气绝/败北动画
- [ ] HUD 是方块条，没有圆角和半透明
- [ ] 有日版富士山/建筑远景、透视水面、透视池壁和中央泳道线

**手感**
- [ ] 命中停顿、闪烁、抖动完全按原版逐帧标定，不自行添加现代化重击演出
- [ ] 跳跃、踢击、受击各自具有准备/有效/恢复相位，并有连续原版证据
- [ ] 肩组、挣扎、拉脚和骑乘使用双人同步clip，双方接触点稳定，无滑动、穿插或脱离
- [ ] 攻击盒、受击盒、抓取点与动画分离；一次攻击不能在同一实例中重复命中同一目标
- [ ] 双方使用同一旧状态快照结算；交换内部更新顺序、同tick交易命中、交互原子中断与固定输入回放hash均通过
- [ ] 败北演出、上浮速度和结果文字时序与原版一致
- [ ] 有 BGM 循环，不只是零散音效
- [ ] 手机上有 FC 手柄样式的虚拟按键，能正常玩

**原版一致性**
- [ ] `ORIGINAL_TUNING` 不再有 `null` 或凭手感填写的常量，每项都有测量记录
- [ ] 使用同一组输入脚本对照参考录像；60Hz证据下关键动作误差不超过1 tick，仅有30fps证据时如实标注约±2 tick
- [ ] 排除定制脸区后的原版/新稿轮廓叠加图已经逐动作验收，未用原创占位帧冒充原版完成
- [ ] 原版模式没有15帧故意延迟、20%打空、技能冷却、挣脱进度等原创机制
- [ ] 池底奖牌、角色能力差异和相关道具若纳入本项目，行为按原版；未纳入时标题明确标注“单项对战版”

**其他**
- [ ] 单个 html 文件，双击直接能开
- [ ] 三张照片按同目录相对路径引用
- [ ] 图片加载失败时有兜底（画个像素小人头，不崩溃）

---

## 13. 给 Kimi 的最后提醒

优先级排序：**原版状态机正确 > 原版输入与判定 > 原版动作轮廓与双人接触 > 原版节奏 > 头像辨识度 > 定制结算**。

禁止为了“更好玩”直接修改 `ORIGINAL`。儿童友好调整全部放到独立的 `KID_ASSIST` 参数层。若时间不足，先砍观众动画、定制结算和触屏装饰，也不能把踢击/肩组出拳合并，不能加入三局两胜、技能式冲刺或连打挣脱槽。

具体开发不得一次性完成，必须严格按照 `输出/给Kimi的分阶段执行指令.md` 分阶段交付。每阶段验收通过后才能进入下一阶段。
