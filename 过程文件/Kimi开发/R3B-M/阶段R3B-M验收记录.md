# 阶段 R3B-M 验收记录（身份编辑蒙版提案）

日期：2026-08-22（含 2026-08-22 最小返修：主HTML 范围保护化） ｜ 基线：HEAD = `3a1c5cb stage3a-r3a-human-master-approved`（开始前只读核验一致）
性质：**仅蒙版提案**，未绘制身份、未接主程序、未提交 Git。等待 Codex 与用户人工审核。

## 1. 指令第九节逐项回答

| # | 问题 | 回答 |
|---|---|---|
| 1 | 每个姿态 L/R 总蒙版像素数 | SURF0 L/R 各 78；JUMP_AIR L/R 各 130；UW_NORMAL L/R 各 38 |
| 2 | 各语义子蒙版像素数 | 见下表 |
| 3 | 蒙版占人体有效像素比例 | SURF0 17.1%（78/457）；JUMP_AIR 9.0%（130/1437）；UW_NORMAL 8.9%（38/425） |
| 4 | 是否完全位于头部内部 | 是。全部蒙版 ⊆ headWin ∩ inner（8 邻域全不透明）∩ y<neckY，机器断言通过 |
| 5 | 手/拳头误覆盖像素数 | 0（六组逐一断言 forbidRects 交集为空；JUMP 举拳区 x≥34 显式为禁区） |
| 6 | 颈肩/身体误覆盖像素数 | 0（y≥neckY 断言为 0；躯干/泳衣/白领禁区交集为 0） |
| 7 | 是否存在第二个面部特征簇 | 否。六组语义簇数均=1（膨胀聚类，gap 按姿态实测：SURF0=3、JUMP=3、UW=4，桥接锁定脸基色行） |
| 8 | 是否修改任何批准母版 | 否。R3A 全部输入以 SHA-256 钉值校验，构建前后一致；changedPixels=0 |
| 9 | 是否修改主HTML | 否。主HTML不是构建输入，仅作范围保护：构建前记录SHA、落盘前复核未变化（仅终端日志，不写入任何输出物）。2026-08-22 返修后不再固定任何特定主HTML SHA |
| 10 | 是否进入身份绘制 | 否。本轮仅蒙版提案，无阳阳/爸爸像素产出 |
| 11 | 是否进入 R3B-I 或 Stage4 | 否。完成后即停止等待审核 |

子蒙版像素数（L/R 相同，R 为镜像）：

| 姿态 | hairOrCap | eyesOrGlasses | faceDetail | mouth |
|---|---|---|---|---|
| SURF0 | 48 | 13 | 9 | 8 |
| JUMP_AIR | 85 | 24 | 7 | 14 |
| UW_NORMAL | 11 | 12 | 10 | 5 |

## 2. 机器自检清单（全部 PASS，显式异常非 assert）

- 6 张蒙版尺寸与对应人体母版完全一致（31×21 / 55×42 / 39×21）
- 蒙版图值域仅 0/255（二值 L 模式 PNG）
- 蒙版 ⊆ opaquePixels（不含透明区/界外像素）
- 蒙版 ⊆ inner（不触及人物外轮廓，不改变 alpha/外轮廓/root/visibleBBox/尺寸）
- 禁区（手/拳/臂、颈肩带、躯干、泳衣/白领、腿脚）交集 = 0
- 颈肩接口（y≥neckY）像素 = 0
- 每张仅一个头部语义簇（无"第二张脸"）
- 语义子蒙版互不重叠，并集 = 总蒙版（无未分类像素）
- L/R 分别导出、分别通过全部断言（R=L 镜像初稿 + 独立校验）
- approved R3A 输入 SHA 与钉值一致（含 contentSha256ByDir 行集复核）
- Stage1/Stage2/R3A 批准文件无修改（钉值校验 + `git diff HEAD` 无输出）
- 主HTML 无修改（范围保护：构建前记录 SHA、落盘前复核一致；非钉值、不参与输出）
- changedPixels = 0
- 两次连续构建全部输出 SHA-256 完全一致
- 干净基线复现：环境A（工作区主HTML 412416e9…）与环境B（Git基线主HTML 44f94a01…）
  均退出 0，34 个产物逐字节完全一致（详见构建复现记录"双环境复现测试"）
- `node --check` / `py_compile` PASS
- 失败路径：任一检查失败即非零退出，不落盘、不覆盖既有输出（已实测：篡改 R3A 输入退出码 1，既有输出 SHA 全部不变）

## 3. 人工目检结论（审核表 + 盲审图）

- 审核表 6 行×10 栏：母版1×/头区1×/蒙版1×/子区1×/叠加1×/锁定1×/母版8×/叠加8×/手拳8×/颈肩8×；1× 为原像素，8× 为单次最近邻放大，无抗锯齿。
- 品红（蒙版）只出现在：发/泳帽顶区、眼窝/镜区、鼻/嘴小区；脸基色保留未圈。
- JUMP_AIR 举拳、SURF0 前伸臂、UW 前后肢：局部放大均无蒙版像素。
- 颈肩接口局部放大无蒙版像素。
- 盲审图 6 张（固定乱序、无姓名/姿态标签）：每张只有一个头部编辑区，无第二脸、无手掌误判、无颈肩/躯干侵入。

## 4. 已知限制与说明

- 蒙版为提案，未消费：`identity_masks_r3bm_baked.js` 未接入任何程序，仅供 R3B-I 批准后使用。
- clusterGap 为按姿态实测的桥接参数（桥接锁定脸基色行），与头相邻部件的防误判由 forbidRects=0 显式承担。
- 盲审图用固定乱序而非真随机（满足双跑 SHA 一致的硬要求）。
- JUMP_AIR 黑色大口洞属头部锁定像素，未纳入蒙版；如需在 R3B-I 中表现口腔，应另行提案。
- 脸基色大面积区域保持锁定（不纳入可编辑范围），身份绘制空间小于 R1 时期的大矩形 ROI，这是有意收窄。

## 5. 2026-08-22 最小返修记录（主HTML 范围保护化）

- 问题：生成器曾把未提交 R6 主HTML 的 SHA（412416e9…）固定为构建输入钉值，导致干净 Git 检出（主HTML SHA=44f94a01…）必然构建失败。
- 修复：`build_identity_masks_r3bm.py` 删除 `MAIN_HTML_PINNED`；主HTML 仅作范围保护（存在检查 + 构建前记录 SHA + 落盘前复核），不参与输出计算、不写入任何输出物。
- **本轮返修没有修改任何蒙版像素**：30 张 PNG、JSON、JS、审核表、盲审图 SHA 与返修前逐字节一致；仅生成器自身 SHA 变为 df6d2c84…（2026-08-22 基线提交桥接又对生成器头部注释做了主HTML相对路径纯注释修正 `../../`→`../../../`，生成器 SHA 最终为 deac9f14…，全部像素/数据产物保持不变）。
- 复现：干净基线主HTML（44f94a01…）与当前工作区主HTML（412416e9…）两个独立环境均可重建完全相同的 R3B-M 产物。
- 六张总蒙版统计保持 SURF0 L/R 各 78、JUMP_AIR L/R 各 130、UW_NORMAL L/R 各 38；changedPixels 仍为 0。

## 6. Git 状态

```
 M .gitignore                      ← 基线前已知现场（本轮未触碰）
 M 阳阳水泳大乱斗-原版复刻.html      ← 基线前已知现场 R6（本轮未触碰）
?? 过程文件/Kimi开发/R3B-M/          ← 本轮唯一新增
（其余 ?? 项为旧 Stage3/3A/3A0 已知现场，本轮未触碰）
```

本轮未执行 git add / commit / reset / checkout / clean，未删除任何旧文件。
