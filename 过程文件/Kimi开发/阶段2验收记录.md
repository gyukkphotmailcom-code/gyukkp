# 阶段2验收记录（第二次小修后）

返修范围：只修阶段2头像；未改主游戏HTML，未执行 git add / git commit。
Git基线：c99e84e stage1-approved-r3。

## 一、第二次小修修改项（对应小修单1~4）

### 1. 左右稿修复（左向 ≠ 右向精确镜像）
左向稿仍以逐行镜像为结构基础，但每组都按**固定左上光源**做了人工修正，身份特征与脸型不变：

| 组别 | 修正内容 | 相对精确镜像差异像素数 |
|---|---|---|
| 阳阳·正常 | 泳镜镜片高光 W 固定在画面左上侧（第16行整行重写） | **6** |
| 阳阳·挨打 | 背光侧（画面右）脸颊边缘 2px S→s 阴影（r17/r18） | **2** |
| 阳阳·气绝 | 张黑嘴右沿、右下沿 2px s→d 加深（r19/r20） | **2** |
| 阳阳·败北 | 背光侧脸颊边缘 2px S→s 阴影（r17/r18） | **2** |
| 爸爸·正常 | 左侧发际 H→h 受光（r4）+ 左镜片上沿 S→W 高光（r10） | **2** |
| 爸爸·挨打 | 同上（r4/r10 未被表情patch占用，修正同样生效） | **2** |
| 爸爸·气绝 | 同上 | **2** |
| 爸爸·败北 | 同上 | **2** |

8 组差异像素数全部 > 0（`diff_vs_mirror()` 输出，运行日志见第六节）。
`validate()` 新增断言：每人每表情 `left != mirror(right)`，违反即构建失败。

### 2. 输出路径修复
- `render_sheet()` 默认输出改为 `Path(__file__).parent / 'avatar_draft.png'`。
- 已从**项目根目录**运行验证：`python3 过程文件/Kimi开发/avatar_paint.py` 只在 `过程文件/Kimi开发/` 下产出草稿，项目根目录无 `avatar_draft.png`（运行前已删除旧散文件复核）。

### 3. 量化底稿分别保存
- `yy_24.png` / `dd_24.png`：原生 24×24 量化图
- `yy_24down.png` / `dd_24down.png`：240×240 最近邻预览
- 以上照片衍生文件连同 `crop_*_ref.png`、含照片截图全部 .gitignore（`git check-ignore -v` 逐项验证命中，见第六节）。

### 4. Base64 安全断言
`build_avatar_page.py` 改为对 `html.lower()` 检查 `data:image` / `base64`，大小写变体亦可拦截。

## 二、识别特征（沿用上轮）

- **阳阳**：儿童圆脸、蓝色泳帽（2档蓝）、泳镜（白镜片+镜带结）、镜片高光、短下巴。
- **爸爸**：成年长脸、棕发+发际线、黑框眼镜（镜框横贯+侧面镜腿）、露齿微笑。

## 三、统一调色板（沿用上轮）

O #000000（PAL.black）/ W #FCFCFC（PAL.white）/ S #FCB88C（PAL.skin）/ s #D88A58（PAL.skinD）/ d #B06840（双方共用统一阴影）；阳阳 C/c #3C9CF0/#1E5AA8，爸爸 H/h #38241A/#5C4030。每人透明+8色键以内。

## 四、四种表情差异（沿用上轮）

| 表情 | 阳阳 | 爸爸 |
|---|---|---|
| 正常 | 泳镜+高光，中性嘴 | 眼镜+瞳孔，露齿微笑 |
| 挨打 | 紧锁眉、X眼、咬牙露齿、3px泪滴汗 | X眼、咬牙、3px泪滴汗 |
| 气绝 | 漩涡眼、2行张黑嘴 | 呆滞方块眼、2行张黑嘴 |
| 败北 | 闭眼低垂、下撇嘴、3px泪滴汗 | 闭眼低垂、败北大口、3px泪滴汗 |

## 五、预览页与隐私（沿用上轮，已重新生成）

- `头像预览.html`：照片外链规定框裁剪（530,480,340,340 / 1214,880,1100,1100 并标注）；1x/3x(72×72验收)/8x 三栏；匿名A/B中性灰底无标识；`#safe` 隐藏全部照片栏；无任何内嵌图片二进制。
- `阶段2头像预览.png`（#safe 安全版）与 `阶段2头像预览-本地含照片.png`（已忽略）均已用本轮数据重新截图。

## 六、验证结果（本轮全部重跑）

### avatar_paint.py 运行日志（项目根目录执行）
```
校验通过: 2人 x 2方向 x 4表情 = 16张, 全部24x24, 色键在调色板内, 左右稿均非精确镜像
草稿图 -> .../过程文件/Kimi开发/avatar_draft.png (1206, 306)
差异像素 yy 正常: 6
差异像素 yy 挨打: 2
差异像素 yy 气绝: 2
差异像素 yy 败北: 2
差异像素 dd 正常: 2
差异像素 dd 挨打: 2
差异像素 dd 气绝: 2
差异像素 dd 败北: 2
```
随后 `ls avatar_draft.png`（项目根）→ No such file or directory ✓

### 量化（avatar_gen.py）
yy 7 色 / dd 6 色，全部落在统一调色板；`yy_24.png`/`dd_24.png`（24×24）与 `*_24down.png`（240×240）分别保存。

### git status --short（未做任何 git 提交操作）
```
 M .gitignore
?? 过程文件/Kimi开发/avatar_draft.png
?? 过程文件/Kimi开发/avatar_gen.py
?? 过程文件/Kimi开发/avatar_paint.py
?? 过程文件/Kimi开发/build_avatar_page.py
?? 过程文件/Kimi开发/头像预览.html
?? 过程文件/Kimi开发/阶段2头像预览.png
?? 过程文件/Kimi开发/阶段2验收记录.md
```
`git check-ignore -v`：yy_24.png、dd_24.png、yy_24down.png、dd_24down.png、crop_yy_ref.png、crop_dd_ref.png、阶段2头像预览-本地含照片.png 全部命中忽略规则。

### data:image / base64 搜索
`头像预览.html`（本轮重新生成）`data:image|base64` 大小写不敏感 grep **零命中**；生成器断言基于 `html.lower()`。

### 16 张校验
尺寸 24×24、用色 6~7 种+透明、透明像素 259~281、方向 {right,left}、表情 4 种——校验脚本与 validate() 双重通过。

## 七、交付物清单

- `过程文件/Kimi开发/头像预览.html`（重新生成）
- `过程文件/Kimi开发/阶段2头像预览.png`（Git安全，重新截图）
- `过程文件/Kimi开发/阶段2头像预览-本地含照片.png`（已忽略，重新截图）
- `过程文件/Kimi开发/avatar_paint.py`（左右稿修正 + 镜像断言 + 差异报告 + 路径修复）
- `过程文件/Kimi开发/avatar_gen.py`（原生24×24与240×240分别保存）
- `过程文件/Kimi开发/build_avatar_page.py`（lower() 断言）
- `过程文件/Kimi开发/avatar_draft.png`（16格草稿拼图）
- 本记录

## 八、仍未实现内容

- 头像未接入主游戏HTML（等确认）。
- 无动画/移动/攻击/AI/声音/触控。
- 头像挂接方式（HUD位置、表情触发条件）待后续阶段定义。

停止，等待 Code Review；不进入阶段3，不做 Git 操作。
